#include <stdlib.h>
#include <signal.h>
#include <stdlib.h>

#include "signals.h"

void init_signals()
{
	/*
	struct sigaction sig;
	sigemptyset(&sig.sa_mask);
	sig.sa_flags = 0;
	sig.sa_handler = SIG_IGN;
	sigaction(SIGINT, &sig, NULL);
	*/
}



